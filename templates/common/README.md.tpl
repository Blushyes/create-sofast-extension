# __DISPLAY_NAME__

This is a Sofast plugin scaffolded by `create-sofast-extension`.

- UI framework: `__FRAMEWORK__`
- UI entry: `__UI_ENTRY__`
- Language: `__EXT__`

Scripts

- `pnpm dev` – start UI dev server via Vite
- `pnpm build` – build UI to `dist/`
- `pnpm preview` – preview built UI

Notes

- This scaffold contains a single command with title "Hello Sofast Extension" to render the UI.
- Update `package.json` top-level `commands` as needed for your use cases.
